package Controller;
/**
 *@author Oleg,Dimitar,Todor.
 *the controller class has the methods that the controller implements.
 */
public interface Controller
{// coooment
   public void execute(String message);
}
